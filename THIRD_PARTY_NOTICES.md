# Third-party notices

The initial POC contains no bundled third-party source or media assets.

Build-time and runtime platform dependencies include Windows SDK 10.0.26100.0,
MSVC, C++/WinRT headers, .NET, and the Windows XAML runtime. They are consumed
from the developer's installed Windows toolchain and are not copied into this
repository.
